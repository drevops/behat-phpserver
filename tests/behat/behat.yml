default:
  suites:
    default:
      contexts:
        - IntegratedExperts\BehatPhpServer\PhpServerContext:
          -
            docroot: %paths.base%/features/fixtures
            host: localhost
            port: 8888
        - FeatureContext
  extensions:
    Behat\MinkExtension:
      sessions:
        default:
          goutte: ~
